<div class="row">
	<% @events.each do |event| %>
		<div class="col-sm-6 col-md-4">
			<div class="thumbnail">
				< % = image_tag event.avatar.url(:thumb) fix later % >
				
				<div class="caption">
					<h3><%= link_to event.name, event_path(event) %></h3>
					<h4>date goes here</h4>
					<h4>venue goes here</h4>
				    <p>Bands: not finished yet!</p>
				    <p><a href="#" class="btn btn-primary" role="button">Button</a>
				    </p>
				</div>
			</div>
		</div>
	<% end %>
</div>

<div class="text-center">
  <p><%= page_entries_info @events %></p>
  <%= paginate @events %>
</div>