<h2>New Event</h2>
<p>Many events have already been submitted to the doindie calendar. Before entering a new event, please take some time to make sure it is not already posted. If the event has already been added, but contains errors or needs updating, please let us know using the contact form and tell us what needs to be changed.</p>

<%= form_for @event do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %><br />
	<%= f.label :contact %>
	<%= f.text_field :contact %><br />
	<%= f.label :flyer %>
	<%= f.file_field :avatar %>
	<%= f.label :price %>
	<%= f.text_field :price %><br />
	<%= f.label :info %>
	<%= f.text_area :info %><br />
	<%= f.label :date %>
	<%= f.date_select :date, start_year: 2014 %><br />
	<%= f.label :time %>
	<%= f.text_field :time, placeholder: "7:40pm" %><br />
	<%= f.label :venue_id %>
	<%= f.select(:venue_id, options_for_select(Venue.order(name: :asc).collect { |v| [v.name, v.id]} )) %><br />
	<%= f.fields_for :event_band do |event_band| %>
		<%= f.label :band %>
		<%= select(:event_band, :band_id, options_for_select(Band.order(name: :asc).collect { |b| [b.name, b.id]} )) %><br />
	<%= f.submit %>
	<% end %>
<% end %>