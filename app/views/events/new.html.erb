<h2>New Event</h2>
<p>Many events have already been submitted to the doindie calendar. Before entering a new event, please take some time to make sure it is not already posted. If the event has already been added, but contains errors or needs updating, please let us know using the contact form and tell us what needs to be changed.</p>

<%= form_for @event do |f| %>
	<%= f.label 'Event Name (English)' %>
	<%= f.text_field :name, class: "form-control half" %><br />
	<%= f.label 'Event Name (Korean)' %>
	<%= f.label "Event Poster" %>
	<%= f.file_field :avatar %><br />
	<%= f.fields_for :event_band do |event_band| %>
		<%= f.label "Bands playing" %>
		<%= select(:event_band, :band_id, options_for_select(Band.order(name: :asc).collect { |b| [b.name, b.id]}.insert(0, "") )) %><br />
	<% end %>
	<p>Additional bands can be added after the event is created</p>
	<%= link_to "Band not listed?", nil %><br />
	<p>only get new band name</p>

	<h4>Venue Details</h4>
	<%= f.label :venue_id %>
	<%= f.select(:venue_id, options_for_select(Venue.order(name: :asc).collect { |v| [v.name, v.id]}.insert(0, "") )) %><br />
	<%= link_to "Venue not listed?", nil %><br />
	<p>get city data from venue relation, new venue form only needs name and city</p></br />

	<%= f.label :date %>
	<%= f.date_select :date, start_year: 2014, class: "form-control half" %><br />
	<%= f.label :time %>
	<%= f.text_field :time, placeholder: "7:40pm", class: "form-control half" %><br />

	<%= f.label "Event Contact" %>
	<%= f.text_field :contact, class: "form-control half" %><br />

	<%= f.label "Event Facebook" %>
	<%= f.text_field :facebook, class: "form-control half" %><br />
	

	<h4>Event Status</h4>
	<%= f.label "Tickets Available" %>
	<%= f.select(:ticket, options_for_select([['Yes', true], ['No', false]], 2)) %><br />

	<%= f.label "Advance Ticket Price" %>
	<%= f.text_field :price, placeholder: "e.g. 10,000 (Do not enter the currency sign)", class: "form-control half" %><br />

	<%= f.label "Door Price" %>
	<%= f.text_field :door_price, placeholder: "e.g. 10,000 (Do not enter the currency sign)", class: "form-control half" %><br />

	<%= f.label "Buy Ticket URL" %>
	<%= f.text_field :ticket_url, class: "form-control half" %><br />

	<%= f.label "English event details" %>
	<%= f.text_area :info, class: "form-control half" %><br />

	<%= f.label "Korean event details" %>
	<%= f.text_area :info_ko, class: "form-control half" %><br />
	
	<%= f.submit %>
	
<% end %>