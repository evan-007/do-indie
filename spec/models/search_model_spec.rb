require 'spec_helper'

describe "SearchModel" do
	context "when no query is present" do
		it "returns all instances of a model" 
	end

	context "when a query is present" do
		it "returns search results"
	end
end